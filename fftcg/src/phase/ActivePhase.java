package phase;

import model.Player;

public class ActivePhase extends Phase{
	
	// Switch all your dull Characters to active.
	// All frozen Characters instead become dull.
	
	void activate(Player player) {
		// Select all cards on the field
		// for (card : field.cards)
		// If owner = player,
		// if (isDull) { isDull = false; }
        // if (isFrozen) { isFrozen = false; isDull = true; }
	}

}
